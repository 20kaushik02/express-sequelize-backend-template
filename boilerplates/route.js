const router = require("express").Router();

const { validate } = require("../validators");

router.get(
	// URL,
	// middleware,
	// validators,
	// validate,
	// controller
);

router.post(
	//similar
);

module.exports = router;
